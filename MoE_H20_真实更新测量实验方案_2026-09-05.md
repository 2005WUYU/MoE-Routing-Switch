# 64 张 H20 条件下的 MoE 真实更新测量实验方案

版本日期　2026 年 9 月 5 日

公开版本说明　本文保留研究方案、算法与假设预算；已移除实际集群清单、网络与调度信息。路径和分区均为待填写的示例值。

研究对象　现有 Qwen3-30B-A3B 权重的全参数语言建模继续训练，重点测量真实优化器更新引起的路由变化、层输出变化及任务损失变化。

交付范围　本文规定研究问题、具体配置、测量算法、资源预算、开发方式及后续扩展边界。文中的运行次数、吞吐情景和资源开销是实验设计与计算结果；没有在该集群执行本文训练，也没有将已有环境探测当作训练性能实测。

## 1. 本轮应当做什么

本轮采用一个模型、一种优化器配置、一条连续训练轨迹。使用已有的约 305 亿参数 Qwen3-30B-A3B，完成 512 次全参数更新，在其中四个位置进行大样本测量，在两个连续窗口记录逐步变化，最后对同一个真实更新做高精度扰动扫描和下游干预。

本轮首先回答一个具体问题：

> 在这个已经训练过的硬 Top-8 MoE 中，正常的一步参数更新到底造成了多少换路由输出变化；这些变化在实际步长下是否影响语言建模损失？

这个问题值得用真实模型、完整上下文和全部 MoE 层认真测量。它不要求同时训练四个宽度、扫描七个学习率、复现十几种稳定方法。将这些任务一起塞入 H20 预算，只会同时削弱训练长度、样本覆盖和测量质量。

本文保留前述研究材料的精确更新分解、路由边界、数值误差和任务影响分析；原方案中的多宽度、多方法、多个种子的训练矩阵，以及 StableMoE 式冻结训练，不列入本轮。[S1–S3]

### 1.1 三类结论分别处理

| 问题 | 本轮可以取得的证据 | 对应工作 |
|---|---|---|
| 路由变化是否产生真实输出差 | 同一输入、相邻真实更新状态、完整新旧专家集合的输出差 | 全部 48 层的一步测量 |
| 这种差是否在任务上有影响 | 相同更新后参数下的完整网络反事实损失，以及指定层的替换实验 | 全网比较和少量明确位置的干预 |
| 是否普遍导致失稳或 μP 迁移失败 | 本轮不能仅凭一个宽度、一条轨迹回答 | 后续 H200 上的多宽度、学习率曲线和独立训练重复 |

第三项留给后续的原因是实验结构不同。提高前两项的测量精度，不能替代第三项需要的独立训练和尺度变化。

### 1.2 本轮的具体规模

| 项目 | 方案 |
|---|---|
| 初始权重 | `/path/to/Qwen3-30B-A3B` |
| 训练方式 | 全参数语言建模继续训练；首次初始化 AdamW 状态 |
| 模型结构 | 原有 48 层、128 专家、Top-8，不删层、不裁专家、不增加共享分支 |
| 真实更新 | 512 步，每步 262,144 个预测位置 |
| 训练 token 数 | 134,217,728，约 1.34 亿 |
| 大样本测量 | 第 64、128、256、512 次更新，各 1,024 条完整序列 |
| 连续窗口 | 第 97–128、481–512 次更新，共 64 个相邻更新；常规窗口每步 64 条序列 |
| 层覆盖 | 每次测量覆盖全部 48 个 MoE 层 |
| 扰动扫描 | 第 511→512 步，8 个 α 值，使用 FP32 主参数和 FP32 全网评估 |
| 下游干预 | 全网旧支持集比较；第 1、24、48 层单层比较；第 24 层增加四个同范数随机方向 |
| GPU 分配 | 64 张 H20；训练与重测轮流使用同一批卡 |
| 默认作业拆分 | 最多三段，每段申请 2 小时 30 分钟；同一条实验轨迹 |
| 默认总申请量 | 7.5 小时 × 64 卡＝480 GPU·小时，不包含另行发生的开发调试 |
| 个人目录占用设计上限 | 按十进制 1 TB＝1,000 GB 计算，包含已有文件及写入中的检查点 |

这里的 512 步是一份可计算资源开销的具体计划，不是声称 512 步就足以证明长期训练规律。已有权重提供了训练过的专家函数；这一段训练提供真实优化器方向、非零参数更新和连续的路由变化记录。

## 2. 作业安排与部署假设

### 2.1 分段执行同一条训练轨迹

本方案按研究预算把同一条训练轨迹拆成三个最多 2.5 小时的作业。实际分区和可申请时限由部署环境填写，不在公开仓库记录具体集群信息。Slurm 作业仍遵循部署环境的调度限制。[R1]

恢复时继续使用检查点中的参数、AdamW 状态、学习率进度、数据位置和随机数状态。重新排队只是执行过程被切开，不算新增一种实验配置，也不算独立随机种子。7.5 小时是计划 GPU 分配时间，不是日历时间承诺。

### 2.2 研究设计的并行布局

以下是拟采用的实验布局，不是实际集群资产清单：64 个 GPU rank，按每组 8 个 rank 组织 EP，普通参数 DP 为 64，专家参数 DP 为 8。第 10 节给出参数、显存和旧激活缓存的计算依据。

节点名称、实际设备清单、网络接口、互连探测、队列状态及环境探测输出均不发布。实际训练吞吐、模型导入和 checkpoint 读写耗时由业务执行记录，不由硬件标称值推定。

### 2.3 存储生命周期

跨作业恢复所需的训练状态写入部署环境提供的共享项目目录。当前作业内的旧激活暂存在节点主存；临时目录不承担唯一持久检查点。第 12 节的 1 TB 是研究预算示例，不陈述某个用户的实际存储使用情况。

## 3. 为什么选择已有 Qwen3，而不是先训练一个很小的 MoE

原问题包含两个条件：专家函数已经存在差异，以及真实学习更新会改变 Router 输入和参数。一个刚随机初始化、只有少量训练的模型，很可能主要反映初始化、专家尚未分化或优化器刚启动的特殊情况。

现有 Qwen3 权重允许把有限时间用在真实更新和精确测量上。官方模型资料给出的结构为 48 层、128 专家、每 token 激活 8 个专家，约 30.5B 总参数和 3.3B 激活参数；这与本地元数据的主体结构一致。[R2]

但本地目录没有原训练的 AdamW 状态，因此本轮的准确表述是：

**从现有权重开始，重新初始化 AdamW 状态，进行全参数语言建模继续训练。**

不能将其写成“恢复原始预训练轨迹”。另外，公开的 `Qwen3-30B-A3B` 与 `Qwen3-30B-A3B-Base` 是不同发布名称；仅靠本地 `config.json`，不能证明当前权重属于未经后训练的 Base 版本。[R2] 本轮使用实际提供的权重，不擅自换成另一个模型，再把替换掩盖在配置里。

### 3.1 不改动原模型的计算含义

本地配置中有一处容易转换错：`hidden_size=2048`，但 `num_attention_heads=32`、`head_dim=128`，所以 Q 投影宽度为 4,096，而不是 2,048；KV 投影宽度为 4×128＝512。Qwen3 的 QK 归一化也应保留。官方 Megatron Bridge 有专门的 Qwen3 MoE 模型支持，适合承担这部分转换。[S4，R3]

保留原有 SwiGLU、RMSNorm、RoPE、QK 归一化、selected normalization 和全部专家。训练时关闭 KV cache，不引入新的 dropout。当前模型没有配置共享专家，本轮不为了对应最初的共享专家问题而临时插入一个。

### 3.2 不在已有权重上临时加一个“μP 开关”

μP 涉及模型族、初始化、参数组更新尺度和宽度变化。本轮只有一个已经训练过的模型。对它临时重缩放矩阵或改变前向乘子，会改变研究对象。

因此，本轮测量真实路由更新，不声称已经实施或验证 μP。以后从头训练多宽度模型时，参数化规则放到另一份实验配置中，继续复用这里的测量函数。

## 4. 一步更新的测量定义

以下定义是本轮的数学定义。读数采用哪种数值实现，另在第 7 节区分，不能混在同一列中。

设第 ℓ 层的稀疏专家输出为

\[
F_\ell(h,\theta;A)
=\sum_{e\in A}g_e(h,\theta;A)E_e(h,\theta),
\qquad
\sum_{e\in A}g_e=1.
\]

对同一完整输入序列，真实更新前后分别得到 \((h_0,\theta_0,A_0)\) 和 \((h_1,\theta_1,A_1)\)。定义

\[
Y_0=F(h_0,\theta_0;A_0),\quad
Y_{1\mid0}=F(h_1,\theta_1;A_0),\quad
Y_1=F(h_1,\theta_1;A_1).
\]

其中 \(Y_{1\mid0}\) 使用旧专家编号，但在新输入、新参数下重新计算旧集合上的门权重。它不保留旧门权重，否则门权重自身的连续更新也会被计入路由集合变化。

记

\[
R=Y_{1\mid0}-Y_0,\qquad D=Y_1-Y_{1\mid0}.
\]

于是

\[
Y_1-Y_0=R+D.
\]

对每个 token 按特征维度 \(d=2048\) 归一化，再按测量输入求平均：

\[
S=\mathbb E\frac{\|R\|^2}{d},\quad
J=\mathbb E\frac{\|D\|^2}{d},\quad
C=\mathbb E\frac{\langle R,D\rangle}{d},
\]

\[
T=\mathbb E\frac{\|R+D\|^2}{d}=S+J+2C.
\tag{1}
\]

**本层的 R 称为“其余更新项”，不直接称为“平滑项”。** 第 24 层的输入变化可能已经包含前面 23 层的路由跳变。本层只重用旧专家集合，不能消除上游的不连续。[S3]

### 4.1 同时报告发生频率和变化幅度

令 \(I=\mathbf 1\{A_1\ne A_0\}\)。在数学定义下，集合不变时 \(D=0\)，因此

\[
J=\Pr(I=1)\,
\mathbb E\!\left[\frac{\|D\|^2}{d}\mid I=1\right].
\tag{2}
\]

这把“换了多少次”与“每次换了多大”分开。大量专家编号变化，可能对应很小的函数变化；少量变化也可能贡献很大的尾部。

主报告包含 \(S,J,C,T\)、切换比例、条件跳变能量和残差归一化 RMS。使用

\[
r_h=\sqrt{J\big/\mathbb E[\|h_1\|^2/d]}
\]

衡量路由变化相对于残差表示的量级。分母为零时报告该比值未定义，不人为加一个常数制造可比较数值。

\(J/T\) 可以大于 1，因为交叉项可能为负。它不是严格意义上相加为 100% 的“贡献占比”。完整解释必须保留式 (1)。

### 4.2 全部变化都按真实专家集合计算

Top-8 一步可能替换不止一个专家。主读数一律比较完整的 \(A_0,A_1\)，不将其近似成第 8 名与第 9 名的一个交换。

第 8/9 名只用于边界分析。先在更新前确定专家编号 i、j，再计算

\[
m_0=z_{0,i}-z_{0,j},\qquad m_1=z_{1,i}-z_{1,j}.
\]

更新后不能重新排序后再取第 8/9 名差值来代替 \(m_1\)，因为重新排序后的差值总是非负。

每层 Router 只有 128 个输出，因此本轮前向时读取完整分数，不用 Top-(K+1) 去猜实际换入专家的旧排名。长期结果只保存所需标量、分布和排名统计，不保存所有 token 的全部分数矩阵。

### 4.3 Router 更新与上游表示更新的精确拆分

当前 Router 无 bias。对旧专家对定义 \(a_0=w_{0,i}-w_{0,j}\)、\(\Delta a=a_1-a_0\)、\(\Delta h=h_1-h_0\)，则

\[
\Delta m
=(\Delta a)^\top h_0+a_0^\top\Delta h+(\Delta a)^\top\Delta h.
\tag{3}
\]

本轮记录三项，不省略最后的交叉项。第一项来自 Router 权重变化，第二项来自上游特征变化。这个分解允许直接观察两条来源，不需要做冻结 Router 的训练对照。

## 5. 路由变化怎样进入任务损失

### 5.1 全网比较

记 \(f(\theta;x)\) 为正常网络，\(f(\theta;x\mid\mathcal A_0)\) 为各层采用更新前专家编号的评估，其中 \(\mathcal A_0\) 对应同一完整输入的全部 token 和全部 MoE 层。

后者在每层仍然根据当前评估路径产生的输入重新计算门权重；上游输出变化后，下游层输入也随之变化，不能直接拿实际新路径的所有中间激活拼装。

定义平均语言建模交叉熵

\[
L_0=L(f(\theta_0;x)),\quad
L_{1\mid0}=L(f(\theta_1;x\mid\mathcal A_0)),\quad
L_1=L(f(\theta_1;x)).
\]

有精确的标量分解

\[
L_1-L_0
=\underbrace{L_{1\mid0}-L_0}_{U}
+\underbrace{L_1-L_{1\mid0}}_{V}.
\tag{4}
\]

\(V>0\) 表示：在这些输入上，以相同更新后参数计算时，重新选择专家比采用旧专家集合有更高的任务损失。

若出现 \(U<0\) 而 \(U+V>0\)，则在这个明确比较中，换路由改变了该步损失变化的符号。这是实际任务影响的直接证据，强于“J 很大”或“路由编号经常变化”。

这项比较通过独立评估函数计算，不回传梯度，不改变后续训练的路由策略。Router、专家、Attention、Embedding 和输出层仍按正常训练目标更新。它不形成一条使用旧路由继续训练的分支。

### 5.2 不把各层损失差直接相加

第 512 步另对第 1、24、48 层分别做单层替换：只在该层采用旧集合，其他层正常计算并重新选择路由。它测量该层改变在真实下游中的影响，包含可能引起的下游路由变化。

三个单层损失差通常不等于全网 V。多层之间存在交互，不能把单层干预结果相加后声称得到了精确总贡献。

第 24 层另比较四个同范数随机方向。每个 token 的随机扰动范数取实际替换向量的范数，方向使用独立生成器；四次评估给出方向对照的均值和范围。实际替换与随机扰动的符号及加入位置必须一致。

这个对照回答“实际路由变化方向是否比普通同大小扰动更影响损失”，不承担稳定方法的有效性证明。

### 5.3 有害量级与统计显著分开

正文报告 V 的绝对单位，采用 nats/token，并报告区间。同步给出相对困惑度变化

\[
\frac{\mathrm{PPL}_1}{\mathrm{PPL}_{1\mid0}}=\exp(V).
\]

例如 \(V=0.001\) 约对应 0.10% 的困惑度变化，\(V=0.01\) 约对应 1.01%。这两个数只帮助解释单位，不作为自动通过或终止实验的阈值。

很窄的区间可以支持“确实存在”，但实际量级可能仍然很小；较大的点估计如果区间很宽，则不能声称已经测准。

这里的抽样区间不包含全部数值误差。实际执行的 V 同时记录同状态重复前向的损失差；第 512 步另有全网 FP32 扫描的端点比较，但其输入轨迹和路由必须按第 8 节独立定义。若 V 的符号落在可观察到的数值差异范围内，只报告符号尚未分清，不能凭大量 token 带来的窄抽样区间认定增损。本层 FP32 的 J 测得准确，也不自动证明全网损失差 V 的数值误差很小。

## 6. 本轮训练配置与输入

### 6.1 一种明确的继续训练配置

| 参数 | 本轮取值 |
|---|---|
| 优化器 | AdamW，全参数；分布式优化器 |
| 首次一、二阶矩 | 零初始化；跨作业恢复时完整保留 |
| 学习率 | 32 步线性 warmup 至 \(10^{-5}\)，之后保持到第 512 步 |
| Adam 系数 | β₁＝0.9，β₂＝0.95，ε＝\(10^{-8}\) |
| Weight decay | 0.1；Norm 和 bias 不做衰减 |
| 梯度裁剪 | 沿用单一全局范数 1.0 的训练规则，记录裁剪前范数和实际缩放 |
| 语言建模损失 | 对有效预测位置取平均 |
| 负载均衡 | 辅助负载均衡损失，系数 0.001；按第 6.2 节定义归一化 |
| 本轮新增路由正则 | 无 |
| 输入序列长度 | 2,048；每条存 2,049 个 token 以构造移位标签 |
| 全局 batch | 128 条序列，即 262,144 个预测位置 |
| 每 GPU microbatch | 1 条序列 |
| 梯度累积 | 2 次，按本轮 DP＝64 计算 |
| 训练精度 | BF16 矩阵计算；FP32 主参数、梯度累积、Adam 状态及 Router 打分计算 |
| 随机输入扰动 | 不增加 |
| Token 丢弃 | 不引入 capacity 丢弃；保留实际负载不均 |

学习率和训练日程是本文提出的继续训练选择，不是恢复原模型的训练配方，也不是已经调出的最优值。这里选一个小的真实继续训练步长，不人为放大学习率来制造跳变。

β₂＝0.95 时，初始二阶矩中零状态的权重按 \(0.95^t\) 衰减，至 128 步约为 0.0014。这说明后半段不再主要由初始零值的权重决定；它不证明梯度分布已经平稳。第 64 步与第 128、256、512 步分别报告，可看到初始适应的变化。

“全参数更新”指所有参数保持可训练且进入适当参数组，不代表稀疏模型每个 token 都给每个专家产生梯度。实际每专家 token 数和空负载次数继续记录。

### 6.2 负载均衡损失也要写清公式

令一条完整序列中用于计算辅助项的有效 token 数为 n，p 为对全部 128 个专家的 softmax 概率。先对每条序列、每一层采用

\[
f_e=\frac1{nK}\sum_u\mathbf1\{e\in A(u)\},\qquad
\bar p_e=\frac1n\sum_u p_e(u),
\]

\[
L_{\mathrm{bal}}=0.001\times M\sum_e f_e\bar p_e.
\tag{5}
\]

式 (5) 先在一条序列内部计算，再对全部 48 层和全局 batch 内的 128 条序列取平均。当前每卡 microbatch 为一条序列，可映射到框架的逐序列辅助损失；正常的数据并行与梯度累积完成序列间平均。这样更换 GPU 数和 microbatch 分组时，不会悄悄改变辅助损失的统计范围。

不同训练框架可能采用逐序列、逐本地 batch 或全局 batch 的辅助项。式 (5) 是本轮选定的具体目标，不声称还原了原始 Qwen 训练目标；接入时明确映射，不把一个数值相同的 0.001 当成所有归一化都相同。这属于训练目标的一部分，不随机器数量自动改动。

### 6.3 数据准备在 GPU 作业之外完成

采用 FineWeb-Edu `sample-10BT` 作为公开来源，按流式方式读取一个有限子集，使用本地模型目录的 tokenizer 重新分词。官方数据卡提供子集和流式读取方法；其 10BT 标记按其他 tokenizer 统计，不能直接当作本轮 Qwen token 数。[R4]

本轮只准备约 2 亿个 Qwen token 的训练文件，以及不重叠的测量输入和文档索引。以 uint32 存储 token，2 亿 token 的主体仅约 0.8 GB。词表大于 65,535，不能使用 uint16。

按原文档标识划分训练池和测量池后再打包序列。记录数据集版本、被使用的分片及顺序、tokenizer 来源、文档标识和打包规则。一次处理有限大小的输入块，处理后释放临时文本，不先下载整个 10BT 子集。

测量输入包含 1,024 条 2,049-token 序列；64 条连续窗口序列取自其中一个预先确定的子集。含 padding 时只统计有效位置。每个 token 的上下文从序列起点完整计算，不单独把边界 token 作为无上下文输入。

序列若共享原文档，在统计时按原文档或相连文档组聚类；不能把同一篇长文切出的多个相邻段落当成完全独立样本。

这里只排除本轮训练池与测量池之间的重叠。现有 Qwen 权重是否在历史训练中见过这些公开文本，当前资料不能确定；本轮也不将结果解释为严格的历史未见数据泛化评估。

## 7. 数值精度：真实执行与高精度函数评估分别记录

本问题研究的量可能远小于激活本身。先把两个 BF16 输出转成 FP32 再相减，并不能恢复相减前已经丢掉的信息。PyTorch 官方文档也明确指出，浮点运算顺序、批处理方式和 TF32 等设置会影响结果。[R5]

因此，每次一步测量保留两个明确区分的读数。

### 7.1 实际执行读数

记 \(F^{\mathrm{exec}}\) 为生产数值路径，包括真实权重精度、专家 GEMM 和聚合输出舍入。使用它计算 \(S^{\mathrm{exec}},J^{\mathrm{exec}},C^{\mathrm{exec}},T^{\mathrm{exec}}\)。

这个读数描述训练实现实际暴露给残差流的变化。新旧集合采用相同 token 分块、同一门规则和相同实现，额外记录相同状态重复前向的差异。不用“相同数学公式”推断两个不同 GEMM 分组一定逐 bit 相等。

### 7.2 本层 FP32 参考读数

在实际捕获的 \(h_0,h_1\) 和实际执行权重数值上，以 FP32 重新计算专家函数与门聚合；真实旧、新专家编号继续作为输入。执行权重原本是 BF16 时，转换为 FP32 表示这些已有数值，不声称恢复了未执行的主参数小数位。

得到 \(S^{\mathrm{ref}},J^{\mathrm{ref}},C^{\mathrm{ref}},T^{\mathrm{ref}}\)。这个读数减少额外评估舍入对函数差的污染，但它是沿实际捕获输入的本层高精度函数比较，不能冒充从 Embedding 开始的全网 FP32 轨迹。

新状态下只需计算专家并集 \(A_0\cup A_1\)，同一个 token 的同一个专家输出算一次，再分别聚合两条路线。两集合相同时，两边引用同一组输出和同一组门值，数学上的零差不会被两次独立计算的舍入差污染。

若 A₀、A₁ 都有 K 个元素且替换 r 个专家，专家并集大小为 K+r，而不是必须计算 2K 个。与计算全部 128 个专家相比，该实现只计算定义真正需要的分支，没有改变测量量。

### 7.3 FP32 参考运算的含义

参考路径关闭 TF32，关闭 autocast，不用 BF16 GEMM 输出冒充 FP32 专家输出。Router、SwiGLU、门归一化和聚合用 FP32；向量差、平方、内积及跨 token 汇总用 FP64。每 token 计算顺序为：先得到两侧 FP32 输出，转换为 FP64，再计算差和能量。

全网 FP32 扰动扫描还需让 Attention、Norm、Embedding 和输出头采用 FP32 数值路径。训练快路径和参考慢路径各自有明确名称，不让调用失败后自动退回另一种精度。

FP64 汇总仍不等于全网 FP64。少量实际维度的专家输入另外与 FP64 参照比较，用于估计 FP32 函数计算误差；这部分不承担分布估计。

### 7.4 从向量误差推导能量误差

若 \(\widehat D=D+e\)，并定义

\[
\varepsilon^2=\mathbb E\frac{\|e\|^2}{d},
\]

由均方范数的三角不等式，

\[
\left|\sqrt{\widehat J}-\sqrt J\right|\le\varepsilon.
\tag{6}
\]

因此可写

\[
J\in
\left[
\max(0,\sqrt{\widehat J}-\varepsilon)^2,
(\sqrt{\widehat J}+\varepsilon)^2
\right].
\tag{7}
\]

式 (7) 只有在 ε 是相应误差界时才是严格包络。由少量 FP64 样本和重复前向得到的 ε 是经验估计，结果中必须按经验数值不确定性报告，不能变成覆盖所有输入的确定性保证。

这里保留误差量和区间，不把噪声能量直接从 J 中相减，也不将落入误差范围的数值自动记成零。

### 7.5 正确性测试与正式测量各做自己的事

开发时使用常数专家解析值、相同专家、相同支持集、完整 selected normalization 和独立全专家求和，核对实现。Mac 上的 CPU/FP64 参考适合这些数学测试；跨卡排序、dispatch 和实际模型转换在集群上调试。

分解恒等式本身只是必要检查。任意错误的中间值 X，都满足 \((X-Y_0)+(Y_1-X)=Y_1-Y_0\)，所以只检查式 (1) 无法证明中间输出算对了。[S3]

这些测试随相关函数开发、修改而执行，正式实验保留实际误差读数，训练配置与中途研究指标的正负、大小无关。

## 8. 测量安排与高精度扰动扫描

### 8.1 测量时刻

训练状态记为 Xₜ，表示已经完成 t 次优化器更新。第 t 次更新的比较始终为 Xₜ₋₁→Xₜ，不比较两个相隔几十步的检查点来冒充“一步更新”。

| 时段 | 使用的序列 | 工作 |
|---|---:|---|
| 第 64 次更新 | 1,024 | 早期继续训练的大样本测量 |
| 第 97–127 次更新 | 每步 64 | 第一个连续窗口的前 31 步 |
| 第 128 次更新 | 1,024 | 大样本测量；其中 64 条同时构成窗口最后一步 |
| 第 256 次更新 | 1,024 | 中间位置的大样本测量 |
| 第 481–511 次更新 | 每步 64 | 第二个连续窗口的前 31 步 |
| 第 512 次更新 | 1,024 | 最后一次大样本测量、窗口最后一步及进一步分析 |

因此并不是 64 次小测量再加四次大测量，而是 **62 次小测量和四次大测量**，不重复计算第 128、512 步的重叠样本。

大样本每次包含

\[
N_{\mathrm{large}}=1024\times2048=2,097,152
\]

个预测位置；小窗口每次包含

\[
N_{\mathrm{small}}=64\times2048=131,072.
\]

全部一步测量共处理

\[
62N_{\mathrm{small}}+4N_{\mathrm{large}}
=16,515,072
\]

个序列位置观测，每个观测覆盖 48 个 MoE 层。这些观测包含重复输入和同一训练轨迹，不能称为 1,651 万个独立实验。

### 8.2 一次测量如何夹在真实更新两侧

一次完整测量按以下顺序执行。

1. 在 Xₜ₋₁ 上对测量序列正常前向，记录旧支持集、所需旧激活及旧输出，同时计算本层 FP32 参考输出。
2. 使用训练数据的下一个完整全局 batch，正常反向并执行一次 AdamW 更新，得到 Xₜ。
3. 对完全相同的测量序列重新从 Embedding 前向，得到新支持集和新输出；在本层新输入上计算旧、新集合的完整输出及 FP32 参考结果。
4. 在更新后参数下，再做一次全网旧支持集评估，计算式 (4) 的任务损失分解。
5. 汇总统计并释放这一测量的临时激活，继续正常训练。

测量数据不进入更新 batch。测量路径不更新负载统计驱动的训练状态，不消耗训练数据游标；随机方向使用单独的随机数生成器。本轮没有新增随机 dropout，因此无需让测量改变训练随机序列。

四个大样本位置使用相同测量输入，适合观察同一输入随训练的变化。统计不确定性按输入文档组计算；轨迹之外的可重复性另由未来的独立训练研究。

### 8.3 只对一个真实方向进行 α 扫描

第 512 次更新前后的 FP32 主参数分别为 θ₀、θ₁。保存真实差

\[
\Delta\theta=\theta_1-\theta_0,
\qquad
\theta(\alpha)=\theta_0+\alpha\Delta\theta.
\]

采用

\[
\alpha\in\left\{0,\frac1{64},\frac1{32},\frac1{16},\frac18,\frac14,\frac12,1\right\}.
\tag{8}
\]

每一点都从 θ₀ 重新构造，不按上一个 α 的结果继续累加。使用独立 FP32 评估模型，从 Embedding 开始运行完整网络。训练器中的 AdamW 状态和第 512 步实际参数不被 α 扫描改写。

FP32 主参数路径与生产 BF16 参数路径不同。特别是

\[
Q_{\mathrm{BF16}}(\theta_0+\alpha\Delta\theta)
\]

随 α 变化可能形成台阶。不能用台阶曲线拟合后，将结果当成连续参数模型的渐近定理。FP32 扫描的 α＝0、1 与生产 BF16 端点分别比较，报告二者差异；不强行要求两套路径的路由相同。

在 α＝0 的 FP32 全网前向中取得该数值模型自己的旧支持集。每个非零 α 同时计算正常网络和采用这套旧支持集的全网评估。不能把生产 BF16 的旧集合当成 FP32 α＝0 必然选出的集合。

### 8.4 平方根规律怎样检验

已有推导是在边界密度、边界位移和专家函数差满足相应条件时，得到

\[
J(\alpha)=\rho\alpha+o(\alpha),
\qquad
\mathrm{RMS}(D)=\sqrt{J(\alpha)}.
\tag{9}
\]

只有 \(0<\rho<\infty\) 时，才出现约 \(\alpha^{1/2}\) 的 RMS。[S3]

本轮首先画全部原始点、事件数和区间，然后计算相邻 α 的能量斜率

\[
b_j=\frac{\log J(\alpha_{j+1})-\log J(\alpha_j)}
{\log\alpha_{j+1}-\log\alpha_j}.
\]

能量斜率 1 对应 RMS 斜率 1/2。零事件点保留为零，不加 ε 后取对数；相邻差异落入数值不确定范围时，斜率按未能分辨处理。

同时检查多专家交换比例、实际换入专家旧排名，以及第 1 层和较深层的差别。较深层的实际输入路径可能包含上游跳变，不能把每层的 R 都拿来拟合“必然为 1 的平滑斜率”。真正固定计算分支的比较使用全网旧支持集路径。

有限个无平局输入通常存在一个很小的 α 区间，其中没有任何样本跨界。因此，本轮没有观察到 1/2 斜率，不等于数学上的硬路由不连续不存在。相反，观察到斜率也不能替代 α＝1 时的实际量级和任务损失测量。

## 9. 怎样判断统计精度

### 9.1 数值精度、样本精度和结论范围是三件事

本轮不压缩专家输出差为一个低精度近似，也不只抽边界 token 来估计总体 J。全部测量序列、全部有效位置、全部 48 层进入相应统计。

但有限样本仍然有抽样误差。一个模型的一百万个 token 不能提供一百万条独立训练轨迹。增加 token 可以帮助估计这条轨迹上的输入分布，不能增加独立种子数量。

### 9.2 切换率的样本量计算

暂时假设 N 个位置独立且切换概率为 p，则

\[
\operatorname{SE}(\hat p)\approx\sqrt{p(1-p)/N},
\qquad
\frac{\operatorname{SE}(\hat p)}p\approx\frac1{\sqrt{Np}}.
\tag{10}
\]

在一次大样本测量的 N＝2,097,152 下：

| 真正切换率 p | 预期事件数 Np | 独立近似下的相对标准误 |
|---:|---:|---:|
| \(10^{-3}\) | 约 2,097 | 约 2.18% |
| \(10^{-4}\) | 约 210 | 约 6.91% |
| \(10^{-5}\) | 约 21 | 约 21.84% |

这个表用于理解样本量，不是实际误差承诺。文档内部 token 的相关性会扩大区间。第 512 步的统计报告同时给出切换发生在多少个独立文档组内，避免把事件集中在几篇长文里的情况隐藏掉。

### 9.3 能量比切换率更难估计

设每个位置的路由能量为 \(Z=IQ\)，其中 Q 为发生切换时的跳变能量，令 \(\mu_Q=\mathbb E[Q\mid I=1]\)、\(\nu_Q=\mathbb E[Q^2\mid I=1]\)。在独立近似下，

\[
\operatorname{Var}(\widehat J)
=\frac{p\nu_Q-p^2\mu_Q^2}{N}.
\]

由此

\[
\frac{\operatorname{Var}(\widehat J)}{J^2}
=\frac1N\left(\frac{\nu_Q}{p\mu_Q^2}-1\right).
\tag{11}
\]

即使切换率测得较准，跳变幅度的长尾仍可能让 J 的区间很宽。因此报告均值、切换条件下的均值、尾部分位数、最大若干事件占总能量的比例，以及文档组重采样区间。

### 9.4 实际采用的统计方式

每个文档组保存有效位置数、能量和、能量平方和、交叉项和、切换计数及三种任务损失和。对文档组进行配对 bootstrap，重新计算比值的分子与分母，不直接平均各组比值。不同 α 使用同一组重采样索引，以保留配对关系。

置信区间写成“条件于本次训练轨迹和所用文本分布的输入抽样区间”。它不包含重新初始化优化器、换一个训练数据顺序或换一个权重来源带来的训练间差异。

每个连续窗口只有 32 次相邻更新，适合展示局部变化和同一步任务影响，不拟合具有普遍预测能力的失稳预警模型。两个窗口之间的几百步不能通过插值伪装成连续观测。

精确经验分位数在内存中计算。对第 512 步及 α 扫描保留足够的逐 token 标量，支持事后核对；其他位置的分位数给出经验值，不声称仅凭均值和方差就能重建其完整分布。

### 9.5 没有测到事件时怎样解释

没有观测到切换时，报告样本数、独立文档组数和零事件。二项模型的约 3/N 上界依赖独立假设，不能直接套在所有 token-layer 上。

没有观测到训练发散时，结论是本段训练没有观测到发散；不能据此给出大规模预训练发散概率的精确上界。存在 J 但 V 接近零，则支持“在本轮输入和更新上，存在输出差但任务影响较小”，不替 J 寻找人为放大的学习率。

## 10. 并行布局、显存和主存的推导

### 10.1 采用节点内 EP＝8

本轮使用

```text
world_size = 64
TP = 1
PP = 1
CP = 1
EP = 8
expert_tensor_parallel_size = 1
sequence_parallel = false
use_distributed_optimizer = true
```

一个节点的八张卡组成一个专家并行组，每张卡持有每层 16 个专家。节点之间对相应专家副本同步梯度；普通参数按数据并行同步。这个选择让 token 的专家分发优先发生在节点内，不引入流水线调度和流水线边界的反事实处理。

在 Megatron 的普通参数并行定义中，

\[
D=\frac{64}{TP\cdot PP\cdot CP}=64.
\]

专家的数据并行副本数为

\[
D_E=D/EP=8.
\]

不能把普通 DP 错写成 8，再把全局 batch 算少八倍。EP 只作用于专家层，不改变普通层的并行定义。[R6]

对应的全局 batch 为

\[
1\ \text{序列/卡/microbatch}\times2\ \text{累积}\times64
=128\ \text{序列}.
\]

进程组按节点连续 rank 布置，保证 EP 组不意外跨节点。主程序启动时输出一次实际分组，作为运行信息；不另开一轮拓扑审计。

### 10.2 每专家看到多少数据

平衡情况下，每层每个专家每步处理

\[
B_E=\frac{BK}{M}
=\frac{262144\times8}{128}=16,384
\]

个 token 路由；512 步共约 8,388,608 个 token 路由/专家/层。实际负载不平衡时逐专家统计真实值。

这说明本轮虽然只训练 512 步，也并非每个专家只见到几个样本。它仍是一段继续训练，不能据此声称所有专家已经在新语料上收敛。

### 10.3 参数量按真实结构计算

从本地权重索引，tensor 数据共 61,064,245,248 字节。按该 BF16 模型配置计算得到

\[
P=30,532,122,624.
\]

其中全部专家参数

\[
P_E=48\times128\times3\times2048\times768
=28,991,029,248,
\]

其他参数

\[
P_D=P-P_E=1,541,093,376.
\]

这与 Embedding/输出头、QKV/O、Router 和归一化参数逐项相加一致。每 rank 常驻的模型参数数目约为

\[
P_{\mathrm{local}}=P_D+P_E/8=5,164,972,032.
\]

### 10.4 分布式优化器的基础显存

Megatron Core 对 BF16 参数、FP32 主梯度和分布式 Adam 状态给出的理论基础开销是每参数 \(6+12/D\) 字节；其中 6 字节来自 BF16 参数与 FP32 梯度，12/D 来自分片主参数和两组矩状态。[R7]

普通参数使用 D＝64，专家参数使用 Dₑ＝8，因此每卡的基础状态显存约为

\[
M_{\mathrm{state}}
=(6+12/64)P_D
+(6+12/8)(P_E/8)
\]

\[
=36.715\ \mathrm{GB}\approx34.19\ \mathrm{GiB}.
\tag{12}
\]

这个数不包含激活、通信 buffer、GEMM workspace、内存分配器保留区和测量临时张量。它证明的是基础状态有明显余量，不是峰值显存已经实测为 34.19 GiB。

训练 microbatch 取 1 条序列。采用框架原生的激活重计算和 grouped GEMM，需要时按固定的重计算配置执行；不靠删层、降低 Top-K 或减少参与更新的模块节省显存。

测量在反向图释放以后运行。局部 FP32 专家权重按层转换或暂存，不同时保留所有层的第二套 GPU 权重。最后的全网 FP32 评估只需前向，其额外本地权重约

\[
4P_{\mathrm{local}}=20.66\ \mathrm{GB}\approx19.24\ \mathrm{GiB}.
\]

无反向图时，基础训练状态加 FP32 评估权重约 53.43 GiB，剩余空间用于单序列评估和通信。FP32 Attention 的实际 workspace 仍计入首次业务执行的峰值记录。

### 10.5 用节点主存保存当前一步的旧激活

一次大测量，单个 FP32 向量字段的全局大小为

\[
2,097,152\times48\times2048\times4
=824.63\ \mathrm{GB}=768\ \mathrm{GiB}.
\]

把所有旧输入、旧输出和差向量都写入磁盘，单次测量就可能超过预算。因此采用以下方式：旧输入按其实际 BF16 位模式保存，旧参考输出用 FP32，必要的旧执行输出用 BF16；分散在各 rank 的 CPU 主存。

三者共 8 字节/特征元素，合计约 1.65 TB **临时主存**，平均每节点约 192 GiB、每 rank 24 GiB。旧专家编号采用 int32，另约 3 GiB 全局。FP32 主参数的分片快照也分散存储，不聚集到 rank 0。

本方案按每节点申请 768 GiB 主存预算，覆盖激活、参数快照、数据缓冲及框架开销。这是资源申请示例，实际可用内存由部署环境确定。

更新后的前向按完整序列逐块处理，取出对应旧记录完成差分，再释放旧记录。高维差向量在计算标量后释放，不继续积累到下一次测量。

## 11. 三段作业与时间预算

### 11.1 默认执行顺序

| 作业 | 训练状态范围 | 测量工作 | 结束时的持久状态 |
|---|---|---|---|
| A | X₀→X₂₅₅ | 第 64、128 步大测量；第 97–128 步连续窗口 | X₂₅₅ 完整检查点 |
| B | 恢复 X₂₅₅，训练至 X₅₁₁ | 第 256 步大测量；第 481–511 步窗口 | X₅₁₁ 完整检查点 |
| C | 恢复 X₅₁₁，执行真实第 512 步 | 最后大测量、窗口末步、FP32 α 扫描、下游干预 | X₅₁₂ 完整检查点与结果 |

C 在更新前保存 FP32 主参数分片，更新后得到同一个真实 Δθ。这样不必额外保存两套全模型参数快照到 NFS，也不必从只包含 BF16 权重的文件猜测真实更新方向。

恢复 X₅₁₁ 后执行第 512 步，不是清空 Adam 状态后重新做一步。数据游标必须指向同一个下一全局 batch。C 的所有比较围绕这一真实更新展开。

每段申请 2.5 小时，共 480 GPU·小时。实际完成后立即退出，不为用满预算继续生成没有设计目的的数据。

### 11.2 先计算工作量，再用真实业务耗时换算

令一个全局训练 batch 的 token 数为 B＝262,144，并定义四个实测时间：

| 符号 | 含义 |
|---|---|
| τₜ | 一个真实训练 step 的时间，含两次累积、通信和 AdamW |
| τᵦ | B 个 token 的完整 BF16 前向，包含测量需要的捕获与传输 |
| τₑ | 在已给定输入上，对 B 个 token 计算全部 48 层 Top-8 专家的 FP32 参考时间 |
| τₚ | B 个 token 的完整 FP32 网络前向 |

本轮一步测量总输入量等于 63B。每份输入最多需要三次完整 BF16 前向，再加本层旧支持集执行；保守按四次完整 BF16 前向估计。FP32 专家参考按旧状态一次、新状态并集最多两次估计。

因此一步测量开销上界式为

\[
T_{\mathrm{pair}}\lesssim252\tau_b+189\tau_e.
\]

α 扫描使用 1 次 α＝0 前向，以及七个非零点各两条全网路径，共 15 个大样本全网 FP32 前向。每个大样本为 8B，因此是 \(120\tau_p\)。本层旧集合补算最多再加 \(56\tau_e\)。

三次单层替换和四次随机方向对照共七个大样本 BF16 前向，即 \(56\tau_b\)。合计

\[
T_{\mathrm{compute}}
\lesssim512\tau_t+308\tau_b+245\tau_e+120\tau_p.
\tag{13}
\]

该式用于预算，不是严格运行时间上界：GEMM 分组变化、负载倾斜、捕获、CPU/GPU 传输和固定启动开销未必随 token 数线性缩放。最终用这些真实工作单元的时间更新估算，不用 H20 峰值 FLOPS 代替。

### 11.3 情景换算，不把估计写成实测

下表中的四种时间全部是说明预算敏感性的假设值。

| 情景 | τₜ | τᵦ | τₑ | τₚ | 式 (13) 的计算时间 |
|---|---:|---:|---:|---:|---:|
| 较快 | 6 s | 2 s | 6 s | 12 s | 约 1.83 小时 |
| 中间 | 10 s | 4 s | 15 s | 30 s | 约 3.79 小时 |
| 较慢 | 16 s | 6 s | 30 s | 60 s | 约 6.83 小时 |

此外还有模型初次导入、两次状态恢复、三次状态保存、启动、编译和统计写出。

以单次完整检查点约 427.45 GB 计算，若有效写入速度分别为 0.5、1、2 GB/s，单次写入约需 14.25、7.12、3.56 分钟。有效速度应包含整个分布式保存的实际结束时间，不取最快 rank 的局部吞吐。

五次完整检查点读写在 0.5 GB/s 情景下约需 71 分钟，初次权重导入另算。因此较慢计算情景已经可能超出默认 7.5 小时，而中间情景有较明显余量。实际执行前不能确定部署环境落在哪一种吞吐情景。

### 11.4 分段时间也要单独计算

除了总时间，还需要看单段是否接近 2.5 小时。按上面的保守计数：

\[
T_A\lesssim255\tau_t+126\tau_b+94.5\tau_e+I_A,
\]

\[
T_B\lesssim256\tau_t+94\tau_b+70.5\tau_e+I_B,
\]

\[
T_C\lesssim\tau_t+88\tau_b+80\tau_e+120\tau_p+I_C.
\tag{14}
\]

其中 I 包含该段状态读写、启动等开销。系数相加恢复式 (13)。C 往往比 A、B 更受 FP32 前向速度限制，因此不能只测训练 tokens/s 就宣布整份方案一定能在三段内完成。

首次业务执行中的训练、测量和保存直接提供这些时间。它们是资源计量，不是额外的准入实验。开发调试消耗单独记在作业记录中，不能隐藏在“训练只用了几小时”之外。

### 11.5 到时怎样结束

每段在正常 step 或完整测量块之间判断剩余预算，预留保存和退出时间；默认预留最后 30 分钟，不在临近结束时启动完整的大样本 α 扫描。使用框架原生的定时退出/保存入口，Slurm 时间上限负责终止超时作业。[R1，R8]

只在当前参数和优化器状态一致的正常更新边界保存。发生程序错误就保留原始报错，在集群调试；不自动降精度、不跳过异常 batch、不自动换一个模型继续跑，也不配置无限重排队。

默认最多三次分配，提交参数关闭自动 requeue。若提前保存，文件中的实际 step 才是恢复依据，不能把计划的 255 或 511 当成已经完成。若实测速度不足，保留已经完成的训练、测量及检查点，明确标注尚未完成的扫描点或训练位置；不能把缺测点补成零或将缺少事件写成否定结果。剩余工作是否另列预算，由新的实验安排决定，本轮不会靠无限续跑兑现一个错误的时长估计。

这里有两个不同保证：Slurm 可以限制资源占用时间；在尚无训练和 NFS 吞吐实测时，不能从硬件型号严格保证全部指定工作一定在该时间内完成。式 (13)、(14) 给出的是可以用实际数据检查和修正的工程预算。

## 12. 1 TB 磁盘预算怎样成立

### 12.1 先把完整训练状态算出来

标准混合精度 AdamW 的持久状态，按每参数保存一份 BF16 执行权重、FP32 主参数、FP32 一阶矩、FP32 二阶矩计算：

\[
2+4+4+4=14\ \text{字节/参数}.
\]

因此一个完整检查点的主体约为

\[
C=14P=427,449,716,736\ \text{字节}
=427.45\ \mathrm{GB}.
\tag{15}
\]

梯度不进入正常更新边界的持久检查点。随机状态、学习率进度、数据游标、tokenizer 和分片元数据另计。具体框架若省略了冗余执行权重，文件可能小于式 (15)，本预算不依赖这种节省。

使用框架原生分布式检查点，每份逻辑参数和优化器状态只保存一份。不能让每个 DP 副本写一份完整 427 GB 文件，也不能写完分片后再额外汇总出一套同样大小的全量文件。[R8]

### 12.2 保留策略只有一个当前状态和一个写入状态

正常情况下只保留一个可恢复的完整检查点。写下一个检查点时，上一份仍在，所以必须按 **两份同时存在** 计算峰值。新保存完成后，释放上一份；下一次保存不会再保留第三份历史状态。

按第 11 节的安排，持久状态依次是 X₂₅₅、X₅₁₁、X₅₁₂。最终保留 X₅₁₂、测量结果与必要元数据。第 64、128、256 步的完整高维激活不长期保存。

这个取舍减少的是事后任意重放所有中间状态的能力，没有降低当时执行的测量精度。要在以后重做另一种中间步骤分析，需要重新训练相应轨迹；不能声称仅凭汇总结果就能恢复所有原始张量。

### 12.3 包含其他文件预留的峰值预算示例

以下统一使用十进制 GB，示例为其他文件预留 45 GB。这些数值是预算假设，不是实际个人目录用量。

| 项目 | 峰值预算 |
|---|---:|
| 其他文件预留（假设） | 45 GB |
| 当前完整检查点 | 427.45 GB |
| 下一份检查点写入中 | 427.45 GB |
| 框架分片对齐、检查点附属文件 | 12 GB |
| 训练文件、测量输入、数据索引 | 8 GB |
| 新增运行环境与必要依赖 | 32 GB |
| 测量标量、日志、报告、少量样例 | 16 GB |
| 其他暂存和结果整理空间 | 16 GB |
| **合计** | **983.90 GB** |
| **到 1,000 GB 的剩余空间** | **16.10 GB** |

983.90 GB 是按指定存储格式和保留数量推导的设计峰值，不是已经在 NFS 实测得到的值。运行环境的 32 GB、分片附属文件的 12 GB 是具体预算项；环境构建时就应选定能放入预算的产物，不把体积未知的完整工具集长期堆在训练目录中。

常态只有一份完整检查点，目录占用明显低于这个峰值。峰值预算主要为下一次正常保存保留空间。

### 12.4 哪些文件不产生

不复制公共目录中的 61 GB 源权重到八个节点自己的 NFS 子目录。优先由 Bridge 从原目录按并行布局导入，避免另存一份永久转换模型；如果采用一次性转换文件，只在初次导入阶段保留，在第一份训练状态生成后释放，不能和两代完整检查点长期共存。[R3，R10]

不保存每次测量的全部高维激活，不保留多个轮次的 profiler 全量 trace，不同时保留几套镜像、Docker 层缓存、SIF 构建目录和 pip 编译缓存。构建发生在训练前，当时没有两份大检查点；结束后只保留本轮实际使用的运行环境。

这些是明确的文件生命周期，不需要在训练循环中不断扫描整个目录、检查配额和阻断任务。保存函数返回实际写入字节数，结果汇总记录一次目录占用即可。

用户在同一路径额外写入与本实验无关的数据，会改变总占用；在没有文件系统硬配额的条件下，本程序不能约束其他进程。本文 1 TB 预算针对“当前已有文件＋本方案全部新增文件”，不把外移到其他个人目录的本实验文件排除出计算。

### 12.5 为什么 16 GB 结果预算够用

不能把 16,515,072×48 个 token-layer 的几十个字段全部写成逐行 JSON。采用二进制数组或列式文件，按以下结构保存。

第一，所有测量位置保存逐文档组的计数、和、平方和、损失和及精确经验分位数；这些数据支持均值、比值和损失差的配对区间。

第二，第 512 步保留六个完整的 float64 标量字段，例如 \(J^{exec},J^{ref},S^{ref},C^{ref},T^{ref},m_0\)。其大小为

\[
2,097,152\times48\times6\times8
=4.83\ \mathrm{GB}.
\]

第三，七个非零 α 各保留全 token-layer 的一个 float64 路由能量字段：

\[
2,097,152\times48\times7\times8
=5.64\ \mathrm{GB}.
\]

两者合计约 10.47 GB，其余空间放索引、逐序列统计、任务损失、分位数、日志和少量向量样例。默认不依赖压缩率才能满足预算。

联合 margin、跳变幅度、换入排名等分布在计算时汇总为明确分箱的计数与矩统计；分箱图属于描述性结果，不替代总体 J 的全样本计算。用于机制案例的高维向量只保存预先选定的小批 token，不把案例样本当作总体估计。

## 13. 代码、机器配置、实验方案分别放在哪里

### 13.1 三种东西不能写在一起

| 层次 | 应当包含 | 不应当包含 |
|---|---|---|
| 通用实现 | 一步状态接口、指定支持集评估、FP32 参考、统计和报告、框架适配 | H20 专用层数、固定 GPU 数、三小时分区名、论文成功阈值 |
| 机器配置 | 设备数量、并行布局、进程启动、容器、路径、单段时限、文件保留数量 | 研究假设、某篇论文的效果判定 |
| 实验配置 | 模型来源、优化器、数据、512 步、采样时刻、α 网格、干预位置 | NCCL 调试命令、特定机器异常恢复流程 |

同一份测量函数不应包含 `if gpu_name == "H20": ...` 来偷偷少测几层。H20 与 H200 的差别进入运行配置和实验配置，数学定义保持一致。

### 13.2 建议的仓库结构

```text
moe-routing-study/
  pyproject.toml
  src/moe_study/
    train.py
    measure.py
    report.py
    metrics.py
    statistics.py
    state.py
    adapters/
      megatron_qwen.py
    reference/
      expert_fp32.py
      network_fp32.py
  tests/
    test_route_aggregation.py
    test_update_decomposition.py
    test_statistics.py
  experiments/
    h20_qwen_real_update.yaml
  machines/
    h20_64.yaml
  schedules/
    h20_three_segments.yaml
  launch/
    slurm.sh
  docs/
    measurement_definition.md
```

`adapters` 只负责把框架中的 Router、专家、dispatch、聚合、主参数和状态接口接到通用测量层。它不决定哪一步有研究价值。

`metrics.py` 接收明确的张量，返回 S/J/C/T 等数值，不负责创建目录或终止作业。`statistics.py` 接收文档组记录，不知道模型跑在 H20 还是 H200。

`state.py` 使用框架原生的分片状态，不另写一个全功能 checkpoint 平台。实现必要的一步快照和恢复接口即可。

### 13.3 本轮实验配置示意

下面是拟实现的项目接口约定，展示字段归属；不是声称这些命令和 YAML 已经存在，也不是可原样传给任意版本 Megatron 的原生参数名。

```yaml
experiment:
  name: qwen3_real_optimizer_update
  model_source: /path/to/Qwen3-30B-A3B
  training_mode: full_parameter_language_modeling
  seed: 20260905
  total_steps: 512

training:
  sequence_length: 2048
  global_sequences_per_step: 128
  optimizer: adamw
  learning_rate: 1.0e-5
  warmup_steps: 32
  schedule_after_warmup: constant
  betas: [0.9, 0.95]
  epsilon: 1.0e-8
  weight_decay: 0.1
  grad_clip_norm: 1.0
  load_balance_coefficient: 0.001
  load_balance_reduction: sequence_then_layer_then_global_batch_mean

measurement:
  layer_selection: all_moe_layers
  large_steps: [64, 128, 256, 512]
  large_sequences: 1024
  consecutive_windows: [[97, 128], [481, 512]]
  window_sequences: 64
  local_outputs: [execution, fp32_reference]
  scalar_reduction_dtype: float64
  recompute_gates_on_old_support: true
  full_network_loss_comparison: true
  interpolation_update: [511, 512]
  alphas: [0.0, 0.015625, 0.03125, 0.0625, 0.125, 0.25, 0.5, 1.0]
  interpolation_parameters: fp32_master
  interpolation_forward: fp32_ieee
  single_layer_interventions: [1, 24, 48]
  random_direction_layer: 24
  random_direction_repeats: 4
```

对应机器配置：

```yaml
machine:
  name: h20_64
  nodes: 8
  gpus_per_node: 8
  partition: YOUR_PARTITION
  walltime_per_job: "02:30:00"
  normal_exit_reserve_minutes: 30
  automatic_requeue: false
  tensor_parallel: 1
  pipeline_parallel: 1
  context_parallel: 1
  expert_parallel: 8
  expert_tensor_parallel: 1
  microbatch_sequences_per_gpu: 1
  gradient_accumulation: 2
  cpu_memory_per_node_gib: 768
  project_root: /path/to/moe-routing-switch
  source_model_read_only: true
  persistent_checkpoint_generations: 1
  simultaneous_checkpoint_generations_during_save: 2
```

训练器读取明确指定的配置，不做多层自动继承和隐式覆盖。开始时输出展开后的实际配置，随后允许正常修改代码和下一份实验方案；配置记录用于说明结果来源，不用来建立“禁止修改”的工作制度。

## 14. Mac 开发与集群调试的具体方式

### 14.1 Mac 上开发什么

Mac 负责项目代码、公式和统计函数、配置、单元测试及结果分析。CPU 环境安装普通 PyTorch、NumPy 和报告依赖，CUDA/TE/Megatron 相关模块延迟到集群入口导入。

Mac 上的小张量参考计算只是软件开发，不将它作为本轮科学实验的替代。Mac 不需要下载 61 GB 模型，也不承担 CUDA、NCCL、专家并行或 H20 吞吐的验证。

开发流程可以保持普通 Git 用法：

```bash
# Mac
cd moe-routing-study
python -m pytest tests/test_route_aggregation.py tests/test_update_decomposition.py

git add src tests experiments machines schedules launch docs

git commit -m "implement paired routing measurements"
git push
```

这几条测试由开发者按修改内容运行，不要求每次提交都全量重跑，也不设置自动阻止启动实验的审批链。

### 14.2 集群拉取和运行

```bash
# 集群登录节点
cd /path/to/moe-routing-switch/code/moe-routing-study
git pull

# 待本文约定的入口实现后，按段提交
sbatch launch/slurm.sh \
  experiments/h20_qwen_real_update.yaml \
  machines/h20_64.yaml \
  schedules/h20_three_segments.yaml A
```

A、B、C 都读取同一实验配置，分别选择自己的恢复位置和结束位置。不会因为分成三个 Slurm job 就重新产生三个 seed 或三个优化器。

### 14.3 集群上直接修复问题

遇到 CUDA、张量布局或通信问题，直接在集群工作目录修改、运行单个有问题的函数或作业片段。修复后提交并 push，Mac 再 pull。

```bash
# 集群完成修复后
git add src
git commit -m "fix expert token ordering in replay"
git push

# Mac
cd moe-routing-study
git pull
```

不在节点启动循环里自动拉取代码，不在训练期间自动部署。修改文件通常不会替换一个已经载入 Python 对象的训练进程；修复需要从适当检查点启动新进程时，正常重启即可。改变了训练计算或测量定义的修复，应在结果里标明生效位置，不把修复前后的两种定义当成同一条无变化的记录。

运行记录保存 commit；存在未提交修复时保存对应 patch。允许调试态运行，不因工作区有改动而拒绝执行。代码不记录模型权重、训练数据、检查点、容器和大量日志，这些都放在 Git 之外。

### 14.4 容器选择

运行环境采用明确版本的配套组件。实际已安装的镜像、驱动、库版本和环境探测记录保留在部署环境，不在公开仓库记录。

本轮开发目标采用一套配套环境，例如官方 `nvcr.io/nvidia/nemo:25.11.01` 对应的 NeMo 25.11 系列。官方组件表给出 PyTorch 2.9.0a0、Megatron Core 0.15 系列、TE 2.9、Bridge 0.2.0 和 CUDA 13.0.1。这里选择的是一个明确组合，并不声称它已经在本集群运行通过。[R9]

在集群的 Linux 环境构建 Apptainer 运行产物，Mac 只编辑构建说明。为本项目只保留一个实际使用的运行环境，按第 12 节的 32 GB 增量预算选择完整 SIF 或只包含训练依赖的构建结果。官方完整工具集若超出该预算，应在环境构建阶段去掉本实验无关组件和缓存；不通过删掉 FP32 主参数、量化优化器或外移隐藏文件腾空间。

环境准备完成后，不在每次训练启动时重新安装依赖。首次实际模型导入和业务执行承担运行适配。

### 14.5 启动脚本只负责启动

主启动形态为每节点一个 Slurm task、task 内 `torchrun` 启动八个进程。请求参数包括：

```text
partition=YOUR_PARTITION
nodes=8
ntasks-per-node=1
gres=gpu:8
cpus-per-task=96
mem=768G
time=02:30:00
no-requeue
```

`torchrun` 使用八个节点和每节点八个进程；节点编号来自 `SLURM_PROCID`，主节点地址来自 Slurm 分配的首节点。项目目录、实际运行环境和源模型目录显式绑定进 Apptainer；源模型目录只读挂载只是保护公共文件，不限制模型参数训练。

NCCL 使用部署环境配置的通信路径；具体网卡、接口及网络日志不在公开仓库记录。仅在排查具体故障时增加 NCCL 日志或指定通信参数。启动脚本不包含自动升级、反复健康巡检、自动改变精度或基于研究指标决定是否放行的逻辑。

## 15. 从结果推导下一步，而不是先安排大量 H20 组别

本轮输出一张按训练位置和层展开的读数表，以及对应的任务损失比较。解释顺序如下。

首先看执行读数与 FP32 本层参考是否相近。如果主要差异来自数值路径，就先把数值因素与函数跳变区分开，不能把它们统一解释成理论中的边界项。

其次看真实 α＝1 的 J、切换条件能量和残差归一化量。α 很小时的斜率描述局部规律，实际一步的任务影响仍以式 (4) 为准。

再看 J 与 V 是否一致。J 明显而 V 很小，表明这个输出方向在当前任务中影响较小；V 明显为正而 J 不突出，则需要看方向和下游放大；J 和 V 都明显时，再结合单层替换定位作用位置。

最后看两段连续窗口，区分一次偶然事件和反复出现的现象。窗口很短时不宣称已经得到长期风险预测器。

| 本轮观测 | 可以直接写出的判断 | 后续真正需要增加的实验 |
|---|---|---|
| J 有明确量级，V 稳定为正 | 本轮模型、数据和真实更新中存在有害路由变化 | 对输出差或边界做训练干预，并做配对独立训练 |
| J 有明确量级，V 接近零且区间较窄 | 输出跳变存在，当前任务影响较小 | 研究其他阶段、文本或更深/更大模型，不强行制造高 LR 失败 |
| 执行读数大，FP32 参考差异明显 | 数值执行对测得变化有重要作用 | 比较数值实现，保持训练目标不变 |
| J、V 都很小，但区间很宽 | 当前数据不足以判清 | 增加独立测量文本或真实更新位置 |
| J、V 都很小，区间也足够窄 | 在明确覆盖范围内，影响较小 | 可以给出限定范围的否定性结论 |
| α 规律存在，但 α＝1 的 V 很小 | 局部几何成立并不等于工程损害 | 将理论性质与训练瓶颈分开写 |

这张表用于结果解释。已经安排的测量照常产生结果，不能根据中途的 J 大小选择性保留样本或改变学习率。

### 15.1 有任务影响后，优先尝试哪种方法

后续最直接的训练候选仍然是前述材料中的边界候选路线输出约束：针对同一个 token 的两条完整 Top-K 聚合，减小它们在真实可能交换区域的输出差。它直接作用于式 (2) 中的条件幅度，而不仅是减少专家编号变化。[S2，S3]

是否让辅助项只回传到专家、是否学习边界权重、是否引入连续化路由，都属于下一份实验方案。任务梯度仍需让所有原有参数正常学习；本轮不把降低学习率到几乎不动、永久固定专家分配或冻结模块当成解决方案。

当前 64 卡计划不附加这条方法的多系数训练网格。把一个约束加进去再训练几步，最多说明代码可以运行，无法替代方法质量、专门化和训练成本的比较。

## 16. 转到 H200 时复用什么，重新设计什么

H200 版本继续复用模型适配器、一步状态捕获、支持集评估、精度区分、S/J/C/T、任务损失比较及统计代码。GPU 型号和数量、分段时限、并行布局、存储保留数量进入新的机器配置。

研究范围扩大时另写实验配置，不在原 H20 配置中逐步塞入大量条件分支。

| 扩展目标 | 改动位置 | 保持不变的部分 |
|---|---|---|
| 更长继续训练 | 步数、数据量、学习率日程和测量时刻 | 一步比较定义、完整状态恢复 |
| 从头训练的多宽度 μP 研究 | 模型族、初始化、参数组规则、宽度列表 | 相同支持集语义下的测量函数 |
| 比较训练干预 | 辅助损失模块和实验配置 | 真实更新、任务比较、系统成本统计 |
| 多个独立 seed | 运行清单和汇总统计 | 每个 run 内的测量协议 |
| 更大 GPU 规模 | TP/PP/CP/EP 和 batch 映射 | 全局 batch、数据语义和损失归一化 |
| 新的 group routing、capacity 或其他路由规则 | 新增或扩展模型适配器 | 已有统计基础，但需明确新的执行状态定义 |

对于同一种受支持的模型和路由形式，迁移应主要是配置变化。新增尚未实现的路由规则仍需要相应适配代码，不能承诺任意架构只改 YAML 就能获得正确反事实。

### 16.1 机器变了，全局训练含义不能悄悄变

若 GPU 数变化，microbatch 与梯度累积可以改变，但全局 token batch、损失归一化、学习率进度和数据位置要显式处理。

例如在 32 卡、其余 TP/PP/CP 不变的运行配置中，为维持每步 128 条序列，累积次数应从 2 改为 4；专家 DP 副本数相应从 8 变为 4。这个配置是以后可以选择的另一种运行安排，本轮不默认偷偷降为 32 卡运行。

硬件、分组和内核改变后，浮点规约顺序可能变化。目标是保持实验定义和可复现的数据处理，而不是承诺 H20 与 H200 的全模型训练逐 bit 相同。[R5]

### 16.2 μP 的完整问题留给真正的多尺度实验

后续若要证明路由更新解释超参数迁移，至少需要明确模型增长路径、在小宽度选择的学习率、较大宽度自己的对照学习率，以及独立训练重复。一个大模型成功继续训练，不能代替学习率迁移曲线。

共享专家和边界训练方法也应在同一计算预算或同一 token 预算下比较，并报告两种预算口径的区别。复用本轮代码解决的是“用同一把测量工具比较”，不是省略这些科学对照。

## 17. 本轮最终应留下的结果

完成本文计划后，项目目录中应留下一个最新完整训练状态、一份实际运行配置及代码版本记录、按文档组保存的统计数据、第 512 步和 α 扫描的逐 token 标量，以及一份结果报告。

报告的主图表为：全部 48 层的真实一步 S/J/C/T；切换频率与条件幅度；残差归一化变化；四个大样本位置的 U/V/总损失变化；两个连续窗口；全部 α 点及事件数；指定层替换与同范数方向比较；实际显存、主存、磁盘峰值和作业耗时。

报告最后直接回答三个问题：本轮是否测到真实函数变化、实际步长下是否影响任务、哪些结论仍需要更多独立训练或宽度变化。未完成的工作按实际位置列出，不把数学推导、CPU 测试和 GPU 训练结果混为一谈。

本轮的资源使用集中在一个真实模型、全部层、完整一步和高精度差分上。以后更换实验规模和研究对照时，继续使用相同的测量实现；H20 的时限和磁盘预算只属于这份具体运行方案。

## 资料与引用

### 用户提供的资料

**[S1]** `MoE_微观更新能量_验证框架与实验设计_2026-09-03.md`。作为问题背景和指标来源；本轮重新限定实验范围和运行安排。

**[S2]** `MoE_muP_硬路由跳变_文献与实验方案_2026-09-03.md`。作为真实一步比较、边界候选路线和后续专家侧方法的背景。本文使用固定专家编号的有符号 margin，不沿用重新排序后差值代表跨界的写法。

**[S3]** `MoE_严格论证_2026-09-05` 文档组，主要参考第 03、04、05、07 篇关于局部/全网分解、任务损失、数值可靠性与实验层次的内容。本文重新设计其运行安排和实验组数。

**[S4]** 模型结构元数据用于研究配置核对；原始集群资料不随公开版本发布。

### 本次核对的官方资料

**[R1] Slurm，sbatch 官方手册。** 支持分区时间限制、作业时限和信号语义。具体部署环境的调度配置不在本文公开。

<https://slurm.schedmd.com/sbatch.html>

**[R2] Qwen 官方模型卡。** 用于核对模型规模、专家数、注意力结构及 Base 与非 Base 发布名称。它不能证明本地权重文件的完整来源。

<https://huggingface.co/Qwen/Qwen3-30B-A3B>

<https://huggingface.co/Qwen/Qwen3-30B-A3B-Base>

**[R3] NVIDIA Megatron Bridge，Qwen3-MoE 文档及版本化 Qwen3-30B-A3B 配方文档。** 支持使用现成的模型转换与训练适配；不提供本集群运行成功的证据。

<https://docs.nvidia.com/nemo/megatron-bridge/latest/models/qwen/qwen3-moe.html>

<https://docs.nvidia.com/nemo/megatron-bridge/0.1.0/apidocs/bridge/bridge.recipes.qwen.qwen3_30b_a3b.html>

**[R4] HuggingFaceFW，FineWeb-Edu 官方数据卡。** 支持公开子集、流式加载和样本版本说明。

<https://huggingface.co/datasets/HuggingFaceFW/fineweb-edu>

**[R5] PyTorch，Numerical accuracy。** 支持浮点运算顺序、批处理与切片差异、TF32 和低精度规约的说明。具体开关按实际运行版本映射。

<https://docs.pytorch.org/docs/main/notes/numerical_accuracy.html>

**[R6] NVIDIA Megatron Bridge，Parallelisms Guide。** 支持普通 DP 的计算方式、EP 只分割专家层以及并行配置语义。

<https://docs.nvidia.com/nemo/megatron-bridge/latest/parallelisms.html>

**[R7] NVIDIA Megatron Core 0.15，Distributed Optimizer。** 支持 BF16 参数、FP32 梯度下基础状态显存 \(6+12/D\) 字节/参数的计算。

<https://docs.nvidia.com/megatron-core/developer-guide/0.15.0/user-guide/features/dist_optimizer.html>

**[R8] NVIDIA Megatron Bridge，Checkpointing。** 支持模型、优化器和随机状态的分布式保存与恢复；实际所用版本的可重分片范围需与其 API 一致。

<https://docs.nvidia.com/nemo/megatron-bridge/latest/training/checkpointing.html>

**[R9] NVIDIA NeMo，Software Component Versions 与 Changelog。** 支持 NeMo 25.11 系列组件配套及 `25.11.01` 发布名称。本文没有在用户集群构建该镜像。

<https://docs.nvidia.com/nemo-framework/user-guide/latest/softwarecomponentversions.html>

<https://docs.nvidia.com/nemo-framework/user-guide/latest/changelog.html>

**[R10] NVIDIA Megatron Bridge，Hugging Face Conversion Guide。** 支持从本地 HF 路径创建模型 provider、设置并行布局及导入权重；具体调用由模型适配器实现。

<https://docs.nvidia.com/nemo/megatron-bridge/latest/bridge-guide.html>

外部资料核对日期为 2026 年 9 月 5 日。本文的 512 步安排、样本量、并行布局选择、工作量公式、文件保留预算和解释表均为本次提出的方案或推导，不是上述官方资料承诺的实验结果。
